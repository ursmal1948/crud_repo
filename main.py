from app.persistence.configuration import players_with_teams_service, player_repo, team_repo
from app.persistence.model import Team, Player
from app.service.players_with_teams import PlayersService


def main() -> None:
    # team = Team(id_=1, name='AA', points=100)
    # player = Player(id_=23, name='new', goals=230, team_id=5)
    # print(players_with_teams_service.change_player_team(player, team))
    pass


if __name__ == '__main__':
    main()
